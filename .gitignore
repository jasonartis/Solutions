# Dependencies
node_modules/

# Env / secrets — never commit
.env
.env.*
!.env.example
!.env.cloud-db.example
!.env.deploy.example
!.env.accounts.example

# Build output
.next/
dist/
out/
.turbo/

# Supabase local
supabase/.temp/
supabase/.branches/

# Logs / OS noise
*.log
.DS_Store
Thumbs.db

# Test artifacts
coverage/
playwright-report/
test-results/

# Vercel local link
.vercel/

# Client-provided source materials (never commit)
client-materials/

# VPS worker env (holds the service-role key)
deploy/worker/worker.env

# Prod database backups (scripts/prod-backup.ts) — never commit data
backups/

# Founder/session scratch — personal, evolving notes and one-off verify
# scripts, deliberately kept out of the repo (CLAUDE.md convention)
/founder-feedback.md
/TESTING-TODO-2DAYS.md
/verify-*.mjs
